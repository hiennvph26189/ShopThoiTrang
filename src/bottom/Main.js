import { Text, View } from "react-native";
import React from "react";
import Header from "../common/Header";
const Main = () => {
    return (
        <View style={{flex:1}}>
            <Header
                title={'Home'}
                
            />
        </View>
    )
}
export default Main;